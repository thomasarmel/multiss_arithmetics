[package]
name = "arithmetics"
version = "0.1.0"
edition = "2021"

[features]
default = ["malachite"]
rug = ["dep:rug"]
malachite = ["dep:malachite"]
bigint = ["dep:num-bigint"]

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "shamir_lagrange"
harness = false

[dependencies]
# Serialization
serde = { version = "1.0.228", features = ["derive"] }

# Math
num-bigint = { version = "0.4.6", optional = true }
rug = { version = "1.28.1", optional = true }
malachite = { version = "0.9.1", features = ["random"], optional = true }
rand = "0.10.0"

# Crypto
rand_chacha = "0.10.0"

# Misc
snafu = "0.8.9"

# Parallelism and concurrency
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
rayon = "1.11.0"
tracing = "0.1.44"
serde_json = "1.0.150"
